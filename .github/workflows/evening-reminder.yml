name: 晚间学习提醒

on:
  schedule:
    # 北京时间每天21:00（UTC 13:00）
    - cron: '0 13 * * *'
  workflow_dispatch:

jobs:
  evening-reminder:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai
    steps:
      - name: Checkout代码
        uses: actions/checkout@v4

      - name: 设置Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: pip install -r requirements.txt

      - name: 确保data目录存在
        run: mkdir -p data

      - name: 配置飞书应用
        env:
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_CHAT_ID: ${{ secrets.FEISHU_CHAT_ID }}
          FEISHU_USER_OPEN_ID: ${{ secrets.FEISHU_USER_OPEN_ID }}
        run: |
          python -c "
          import json, os
          config = {
            'project_name': 'interview-prep-system',
            'version': '1.0.0',
            'data_dir': 'data',
            'questions_file': 'data/questions.json',
            'companies_file': 'data/companies.json',
            'progress_file': 'data/progress.json',
            'daily_log_file': 'data/daily_log.json',
            'feishu': {
              'enabled': True,
              'app_id': os.environ['FEISHU_APP_ID'],
              'app_secret': os.environ['FEISHU_APP_SECRET'],
              'chat_id': os.environ['FEISHU_CHAT_ID'],
              'chat_name': 'lsn',
              'user_open_id': os.environ['FEISHU_USER_OPEN_ID'],
              'daily_push_time': '09:00',
              'evening_reminder_time': '21:00',
              'message_template': 'daily'
            },
            'schedule': {
              'daily_push_cron': '0 9 * * *',
              'evening_reminder_cron': '0 21 * * *',
              'weekly_update_cron': '0 10 * * 0',
              'monthly_review_cron': '0 10 1 * *'
            },
            'question_selection': {
              'daily_count': 3,
              'strategy': 'adaptive',
              'rotation_categories': ['算法', '操作系统', '计算机网络', '数据库', 'C++', 'Java', '系统设计', '具身智能', 'AI大模型'],
              'review_interval_days': [1, 3, 7, 14, 30],
              'weak_area_boost': 1.5
            }
          }
          with open('config.json', 'w', encoding='utf-8') as f:
            json.dump(config, f, ensure_ascii=False, indent=2)
          print('config.json 写入成功')
          "

      - name: 发送晚间提醒
        run: python scripts/feishu_push.py --mode evening
